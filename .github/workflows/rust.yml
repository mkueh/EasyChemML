name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Find Cargo.toml
      run: echo "${{  find $GITHUB_WORKSPACE -name "Cargo.toml" -exec basename {} \; }}"


